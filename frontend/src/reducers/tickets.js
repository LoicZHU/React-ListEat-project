// initial state
const initialState = {
  
};

// reducer
const ticketsReducer = (state = initialState, action = {}) => {
  switch (action.type) {
    default: return state;
  }
};


// export
export default ticketsReducer;
