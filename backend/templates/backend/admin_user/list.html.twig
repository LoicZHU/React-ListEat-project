{% extends 'base.html.twig' %}

{% block title %}Users admin list{% endblock %}

{% block body %}



    </div>
        <div class="content">
            <div class="row">
                <div class="col-md-12">
                    <div class="card">
                        <div class="card-header">
                            <h4 class="card-title">User</h4>
                        </div>
                        
                        <div class="card-body">
                            <div class="table-responsive">
                                <table class="table">
                                    <thead class=" text-primary">
                                    <th>
                                        Id
                                    </th>
                                    <th>
                                        First name
                                    </th>
                                    <th>
                                        Last name
                                    </th>
                                    <th>
                                        restaurant
                                    </th>
                                    <th class="text-right">
                                        email
                                    </th>
                                    </thead>
                                    <tbody>
                                    {% for user in users %}
                                    <tr>
                                        <td>
                                        {{ user.id }}
                                        </td>
                                        <td>
                                        {{ user.firstName }}
                                        </td>
                                        <td>
                                        {{ user.lastName }}
                                        </td>
                                        <td>
                                        {{ user.role.name }}
                                        </td>
                                        <td class="text-right">
                                        {{ user.email }}
                                        </td>
                                         <td>
                                            <a href="{{ path('backend_user_delete', {'id': user.id}) }}">show</a>
                                            <a href="{{ path('backend_user_delete', {'id': user.id}) }}">edit</a>
                                        </td>
                                        <td>
                                        <form action="{{ path('backend_user_delete', {id: user.id}) }}" method="post" onsubmit="return confirm('Supprimer l'utilisateur {{ user.firstName|e('js') }} ?')">
                
                                            {# the argument of csrf_token() is an arbitrary string used to generate the token #}
                                          
                                            {# Modification (surcharge) de la méthode HTTP #}
                                            <input type="hidden" name="_method" value="DELETE" />

                                            <button type="submit">supprimer</button>
                                        </form>
                        
                                        </td>
                                    </tr>
                                    {% endfor %}
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>




{% endblock %}
